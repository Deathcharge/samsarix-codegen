{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://samsarix.com/schemas/codegen/execution-plan-verification-v2.schema.json",
  "title": "Samsarix Codegen execution-plan verification",
  "description": "Content-omitting evidence that one execution plan matches one validated request artifact and identifies its bound result-policy approval.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "plan_fingerprint",
    "result_policy_fingerprint",
    "request",
    "provider",
    "budgets"
  ],
  "properties": {
    "schema_version": {"const": 2},
    "plan_fingerprint": {"$ref": "#/$defs/fingerprint"},
    "result_policy_fingerprint": {
      "oneOf": [
        {"$ref": "#/$defs/fingerprint"},
        {"type": "null"}
      ]
    },
    "request": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "fingerprint",
        "messages",
        "context_items",
        "context_bytes",
        "estimated_input_tokens"
      ],
      "properties": {
        "fingerprint": {"$ref": "#/$defs/fingerprint"},
        "messages": {"type": "integer", "minimum": 1, "maximum": 32},
        "context_items": {"type": "integer", "minimum": 0, "maximum": 100},
        "context_bytes": {"type": "integer", "minimum": 0},
        "estimated_input_tokens": {"type": "integer", "minimum": 1, "maximum": 2000000}
      }
    },
    "provider": {"$ref": "#/$defs/provider"},
    "budgets": {
      "type": "object",
      "additionalProperties": false,
      "required": ["max_estimated_input_tokens", "remaining_estimated_input_tokens"],
      "properties": {
        "max_estimated_input_tokens": {
          "type": "integer",
          "minimum": 1,
          "maximum": 2000000
        },
        "remaining_estimated_input_tokens": {
          "type": "integer",
          "minimum": 0,
          "maximum": 2000000
        }
      }
    }
  },
  "$defs": {
    "fingerprint": {
      "type": "string",
      "pattern": "^sha256:[0-9a-f]{64}$"
    },
    "provider": {
      "type": "object",
      "additionalProperties": false,
      "required": ["endpoint", "model", "timeout_seconds", "max_output_tokens"],
      "properties": {
        "endpoint": {
          "type": "string",
          "minLength": 1,
          "maxLength": 2048,
          "anyOf": [
            {
              "pattern": "^https://(?:[^:/@?#\\s]+|\\[[0-9A-Fa-f:.]+\\])(?::[0-9]{1,5})?(?:/[^?#\\s]*)?$"
            },
            {
              "pattern": "^http://(?:localhost|127\\.(?:(?:25[0-5]|2[0-4][0-9]|1[0-9]{2}|[1-9]?[0-9])\\.){2}(?:25[0-5]|2[0-4][0-9]|1[0-9]{2}|[1-9]?[0-9])|\\[::1\\])(?::[0-9]{1,5})?(?:/[^?#\\s]*)?$"
            }
          ]
        },
        "model": {
          "type": "string",
          "minLength": 1,
          "maxLength": 200,
          "pattern": "^[^\\s\\u0000-\\u001f\\u007f](?:[^\\u0000-\\u001f\\u007f]*[^\\s\\u0000-\\u001f\\u007f])?$"
        },
        "timeout_seconds": {"type": "integer", "minimum": 1, "maximum": 300},
        "max_output_tokens": {"type": "integer", "minimum": 1, "maximum": 32768}
      }
    }
  }
}
